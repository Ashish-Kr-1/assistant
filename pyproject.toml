[project]
name = "charaka-ip"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # ── existing ──────────────────────────────────────────────────────────────
    "fastapi>=0.121.0",
    "langchain>=1.4.0",
    "langchain-cohere>=0.6.0",
    "langchain-openai>=1.6.2",
    "langchain-tavily>=0.2.18",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
    "uvicorn[standard]>=0.38.0",
    # ── Phase 1: orchestration ────────────────────────────────────────────────
    "langgraph>=0.2.0",
    # ── Phase 1: corpus ingestion ─────────────────────────────────────────────
    "chromadb>=0.6.0",
    "sentence-transformers>=3.0.0",
    "beautifulsoup4>=4.12.0",
    "pypdf>=5.0.0",
    "httpx>=0.28.0",
    "lxml>=5.0.0",
    # ── T3: hybrid search ─────────────────────────────────────────────────────
    "rank-bm25>=0.2.2",
    # ── T7: semantic cache ────────────────────────────────────────────────────
    "redis>=5.0.0",
    # ── legacy assistant backend/ml_pipeline ──────────────────────────────────
    "psycopg2-binary>=2.9.13",
    "sqlalchemy>=2.0.52",
    "pydantic-settings>=2.1.0",
    "qdrant-client>=1.7.0",
    "neo4j>=5.16.0",
    "tiktoken>=0.5.2",
    "python-multipart>=0.0.6",
    "cryptography>=42.0.0",
    "networkx>=3.2.1",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
